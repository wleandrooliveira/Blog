<%= form_for @comment, html: { class: 'form' } do |f| %>
  <div class="form-group">
    <%= f.label :content, "Comentário" %>
    <%= f.text_area :content, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :post %>
    <%= collection_select(:comment, :post_id, Post.all, :id, :title) %>
  </div>
  <div class="form-group">
      <td>
        <%= f.submit 'Salvar', :class => 'btn btn-success'%>
        <%= link_to 'Voltar', comments_path, class: 'btn btn-small btn-primary' %>
      </td>
  </div>
<% end %>
